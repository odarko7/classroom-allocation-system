services:
  - type: web
    name: classroom-allocation
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm ci && cd ../frontend && npm ci && npm run build && cd ../backend
    startCommand: node src/index.ts
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "24"
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        sync: false
      - key: SEED_DEMO_DATA
        value: "true"
